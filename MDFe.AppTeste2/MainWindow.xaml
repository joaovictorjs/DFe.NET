<mah:MetroWindow
  x:Class="MDFe.AppTeste2.MainWindow"
  xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:local="clr-namespace:MDFe.AppTeste2"
  xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
  xmlns:views="clr-namespace:MDFe.AppTeste2.Views"
  xmlns:viewmodels="clr-namespace:MDFe.AppTeste2.ViewModels"
  d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
  mc:Ignorable="d"
  Title="Zeus - MDFe app testes"
  WindowStartupLocation="CenterScreen"
  Height="600"
  Width="800"
>
  <mah:MetroWindow.LeftWindowCommands>
    <mah:WindowCommands>
      <Button>
        <iconPacks:PackIconLucide Kind="Truck"></iconPacks:PackIconLucide>
      </Button>
    </mah:WindowCommands>
  </mah:MetroWindow.LeftWindowCommands>

  <mah:MetroWindow.RightWindowCommands>
    <mah:WindowCommands>
      <Button Command="{Binding AbrirConfiguracoesCommand}">
        <Button.ContentTemplate>
          <DataTemplate>
            <StackPanel Orientation="Horizontal">
              <iconPacks:PackIconLucide Kind="Settings" />
              <TextBlock Margin="8,0">Configurações</TextBlock>
            </StackPanel>
          </DataTemplate>
        </Button.ContentTemplate>
      </Button>
    </mah:WindowCommands>
  </mah:MetroWindow.RightWindowCommands>

  <mah:MetroWindow.Flyouts>
    <mah:FlyoutsControl>
      <views:ConfiguracoesView IsOpen="{Binding ConfiguracoesAberta, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
    </mah:FlyoutsControl>
  </mah:MetroWindow.Flyouts>

  <Grid></Grid>
</mah:MetroWindow>
